apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  type: ClustorIP
  ports:
    - name: "http-port"
      protocol: "TCP"
      port: 5000
      targetPort: 5000
    selector:
      app: flask-app
